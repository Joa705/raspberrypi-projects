# FastAPI Backend Requirements
# Python dependencies for the Raspberry Pi hardware control backend
# Compatible with Python 3.13+

# Core FastAPI dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.0.0

# WebRTC streaming dependencies
aiortc>=1.9.0  # WebRTC implementation for Python
av>=12.0.0  # PyAV for media processing (required by aiortc)
aiohttp>=3.9.0  # Async HTTP (required by aiortc)

# Camera streaming dependencies (kept for backwards compatibility)
opencv-python-headless>=4.10.0  # For RTSP camera streaming (headless for servers)

# System monitoring
psutil>=5.9.0  # System and process utilities

# Database
sqlalchemy==2.0.36
aiosqlite==0.20.0
greenlet>=3.0.0  # Required for SQLAlchemy async operations

# Environment Variables
python-dotenv==1.0.0

# Authentication
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-jose[cryptography]
python-multipart

# Optional: Add these when ready to connect real hardware
# RPi.GPIO==0.7.1  # For GPIO control (relay, sensors)
# picamera2  # For Raspberry Pi camera
# adafruit-circuitpython-dht  # For DHT temperature sensors